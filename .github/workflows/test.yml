name: Check example code

on:
  push:
    branches:
      - master
  pull_request:
    branches:
      - master

jobs:
  correctness:
    name: OI-wiki Example Code Correctness Check
    runs-on: ubuntu-latest
    outputs:
      any_changed: ${{ steps.changed-files.outputs.any_changed }}
      files_to_test: ${{ steps.GetFiles.outputs.files_to_test }}
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Get changed code files
        id: changed-files
        uses: tj-actions/changed-files@v42
        with:
          files: |
            docs/**/*.cpp
            docs/**/*.in
            docs/**/*.ans
      - uses: actions/setup-python@v5
        if: steps.changed-files.outputs.any_changed == 'true'
        with:
          python-version: '3.10'
          cache: pipenv
      - name: Install Python dependencies
        if: steps.changed-files.outputs.any_changed == 'true'
        run: |
          curl https://raw.githubusercontent.com/pypa/pipenv/master/get-pipenv.py | python3
          pipenv install
      - name: Install C++
        if: steps.changed-files.outputs.any_changed == 'true'
        run: sudo apt-get install -y g++
      - name: Make the File list
        if: steps.changed-files.outputs.any_changed == 'true'
        run: echo "${{ steps.changed-files.outputs.all_changed_files }}" > res.txt
      - name: Get Files to Test
        if: steps.changed-files.outputs.any_changed == 'true'
        id: GetFiles
        run: pipenv run python scripts/get_files_to_test.py
      - name: Correctness Check
        if: steps.changed-files.outputs.any_changed == 'true'
        env: 
          FILES_TO_TEST: ${{ steps.GetFiles.outputs.files_to_test }}
        run: pipenv run python scripts/correctness_check.py

  undefinedbehaviors-ubuntu:
    name: OI-wiki Undefined Behaviors Test - Ubuntu
    needs: correctness
    if: ${{ needs.correctness.outputs.any_changed == 'true' }}
    runs-on: ubuntu-latest
    steps:
      - name: check output
        env: 
          FILES_TO_TEST: ${{ needs.correctness.outputs.files_to_test }}
        run: echo "${FILES_TO_TEST}"
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Get changed code files
        id: changed-files
        uses: tj-actions/changed-files@v42
        with:
          files: |
            docs/**/*.cpp
            docs/**/*.in
            docs/**/*.ans
      - uses: actions/setup-python@v5
        if: steps.changed-files.outputs.any_changed == 'true'
        with:
          python-version: '3.10'
          cache: pipenv
      - name: Install Python dependencies
        if: steps.changed-files.outputs.any_changed == 'true'
        run: |
          curl https://raw.githubusercontent.com/pypa/pipenv/master/get-pipenv.py | python3
          pipenv install
      - name: Install GCC, G++, and Musl Toolchains
        if: steps.changed-files.outputs.any_changed == 'true'
        run: |
          sudo apt-get install -y gcc-10 g++-10 gcc-9 g++-9
          g++-10 --version
          g++-9 --version
      - name: Make the File list
        if: steps.changed-files.outputs.any_changed == 'true'
        run: echo "${{ steps.changed-files.outputs.all_changed_files }}" > res.txt
      - name: Undefined Behaviors Check
        if: steps.changed-files.outputs.any_changed == 'true'
        env: 
          FILES_TO_TEST: ${{ needs.correctness.outputs.files_to_test }}
          RUNS_ON: ubuntu-latest
        run: pipenv run python scripts/ub_check_ubuntu.py

  undefinedbehaviors-macos:
    name: OI-wiki Undefined Behaviors Test - MacOS
    needs: correctness
    if: ${{ needs.correctness.outputs.any_changed == 'true' }}
    runs-on: macos-latest
    steps:
      - name: check output
        env: 
          FILES_TO_TEST: ${{ needs.correctness.outputs.files_to_test }}
        run: echo "${FILES_TO_TEST}"
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Get changed code files
        id: changed-files
        uses: tj-actions/changed-files@v42
        with:
          files: |
            docs/**/*.cpp
            docs/**/*.in
            docs/**/*.ans
      - uses: actions/setup-python@v5
        if: steps.changed-files.outputs.any_changed == 'true'
        with:
          python-version: '3.10'
          cache: pipenv
      - name: Install Python dependencies
        if: steps.changed-files.outputs.any_changed == 'true'
        run: |
          curl https://raw.githubusercontent.com/pypa/pipenv/master/get-pipenv.py | python3
          pipenv install
      - name: Install C++
        if: steps.changed-files.outputs.any_changed == 'true'
        run: |
          brew install gcc@14
      - name: g++ Version
        if: steps.changed-files.outputs.any_changed == 'true'
        run: | 
          g++-14 --version
          clang --version
      - name: Make the File list
        if: steps.changed-files.outputs.any_changed == 'true'
        run: echo "${{ steps.changed-files.outputs.all_changed_files }}" > res.txt
      - name: Undefined Behaviors Check
        if: steps.changed-files.outputs.any_changed == 'true'
        env: 
          FILES_TO_TEST: ${{ needs.correctness.outputs.files_to_test }}
          RUNS_ON: macos-latest
        run: pipenv run python scripts/ub_check_macos.py

  undefinedbehaviors-rv:
    name: OI-wiki Undefined Behaviors Test - RISC-V
    needs: correctness
    if: ${{ needs.correctness.outputs.any_changed == 'true' }}
    runs-on: riscv-builders
    steps:
      - name: check output
        env: 
          FILES_TO_TEST: ${{ needs.correctness.outputs.files_to_test }}
        run: echo "${FILES_TO_TEST}"
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Get changed code files
        id: changed-files
        uses: tj-actions/changed-files@v42
        with:
          files: |
            docs/**/*.cpp
            docs/**/*.in
            docs/**/*.ans
      # - uses: actions/setup-python@v5
      #   if: steps.changed-files.outputs.any_changed == 'true'
      #   with:
      #     python-version: '3.10'
      #     cache: pipenv
      # - name: Install Python dependencies
      #   if: steps.changed-files.outputs.any_changed == 'true'
      #   run: |
      #     curl https://raw.githubusercontent.com/pypa/pipenv/master/get-pipenv.py | python3
      #     pipenv install
      - name: Install Clang
        if: steps.changed-files.outputs.any_changed == 'true'
        run: apt install -y clang
      - name: Make the File list
        if: steps.changed-files.outputs.any_changed == 'true'
        run: echo "${{ steps.changed-files.outputs.all_changed_files }}" > res.txt
      - name: Undefined Behaviors Check
        if: steps.changed-files.outputs.any_changed == 'true'
        env: 
          FILES_TO_TEST: ${{ needs.correctness.outputs.files_to_test }}
          RUNS_ON: riscv-builders
        run: python3 scripts/ub_check_riscv.py

  undefinedbehaviors-windows:
    name: OI-wiki Undefined Behaviors Test - Windows
    needs: correctness
    if: ${{ needs.correctness.outputs.any_changed == 'true' }}
    runs-on: windows-latest
    steps:
      - name: check output
        env: 
          FILES_TO_TEST: ${{ needs.correctness.outputs.files_to_test }}
        run: echo "${FILES_TO_TEST}"
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Get changed code files
        id: changed-files
        uses: tj-actions/changed-files@v42
        with:
          files: |
            docs/**/*.cpp
            docs/**/*.in
            docs/**/*.ans
      - uses: actions/setup-python@v5
        if: steps.changed-files.outputs.any_changed == 'true'
        with:
          python-version: '3.10'
          cache: pipenv
      - name: Install Python dependencies
        if: steps.changed-files.outputs.any_changed == 'true'
        run: |
          curl https://raw.githubusercontent.com/pypa/pipenv/master/get-pipenv.py | python3
          pipenv install
      - name: Install Clang, GCC, G++; Detect Toolchains
        if: steps.changed-files.outputs.any_changed == 'true'
        run: |
          choco install -y llvm
          choco install -y mingw
          clang --version
          g++ --version
          "C:\Program Files\Microsoft Visual Studio\2022\Enterprise\VC\Auxiliary\Build\vcvars64.bat"
          cl
      - name: Make the File list
        if: steps.changed-files.outputs.any_changed == 'true'
        run: echo "${{ steps.changed-files.outputs.all_changed_files }}" > res.txt
      # - name: Undefined Behaviors Check
      #   if: steps.changed-files.outputs.any_changed == 'true'
      #   env: 
      #     FILES_TO_TEST: ${{ needs.correctness.outputs.files_to_test }}
      #     RUNS_ON: windows-latest
      #   run: python scripts/ub_check_windows.py